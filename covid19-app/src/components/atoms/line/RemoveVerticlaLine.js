import React from 'react';
import styled from 'styled-components';

const StyledRemoveVerticalLine = styled.div`
  height: 14px;
  width: 1px;
`;

function RemoveVerticalLine() {
  return <StyledRemoveVerticalLine />;
}

export default RemoveVerticalLine;

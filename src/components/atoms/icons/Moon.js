import { BsMoon } from 'react-icons/bs';
import styled from 'styled-components';

export const StyledMoonDark = styled(BsMoon)`
  font-size: 20px;

  &:hover {
    color: #778ca3;
    transform: scale(1.5);
  }
`;
